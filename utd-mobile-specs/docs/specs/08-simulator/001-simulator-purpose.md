# Simulator Purpose

The simulator is first-class and mandatory.

It must make balancing viable by running deterministic headless simulations using `game-core`, not Phaser.

Do not rely primarily on generic AI bot players. Use scripted strategy profiles and controlled scenarios.
